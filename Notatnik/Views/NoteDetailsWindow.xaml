<Window x:Class="Notatnik.Views.NoteDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:Notatnik.Models"
        Title="Szczegóły notatki" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="10">

        <!-- Przyciski Zapisz / Anuluj -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Zapisz" Command="{Binding SaveCommand}" Width="75" Margin="5,0"/>
            <Button Content="Anuluj" Command="{Binding CancelCommand}" Width="75" Margin="5,0"/>
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Tytuł -->
                <TextBlock Text="Tytuł:" FontWeight="Bold"/>
                <TextBox Text="{Binding Note.Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <!-- Panel tylko dla CheckList -->
                <StackPanel x:Name="CheckListPanel" Margin="0,0,0,10">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Note.Type}" Value="{x:Static models:NoteType.CheckList}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="Elementy listy:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ItemsControl ItemsSource="{Binding ChecklistItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center"/>
                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="380" Margin="5,0"/>
                                    <Button Content="X"
                                            Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Width="20" Height="20" Margin="5,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button Content="Dodaj element"
                            Command="{Binding AddItemCommand}"
                            Margin="0,5" Width="100"/>
                </StackPanel>

                <!-- Pozostałe panele (zostawione na później) -->
                <StackPanel x:Name="RegularPanel" Visibility="Collapsed"/>
                <StackPanel x:Name="LongFormatPanel" Visibility="Collapsed"/>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
